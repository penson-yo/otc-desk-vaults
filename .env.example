# Server + /api/rpc proxy. Defaults to publicnode, then Solana Foundation.
# SOLANA_RPC_URL=
# Optional. Overrides the browser wallet RPC (default is same-origin /api/rpc).
# NEXT_PUBLIC_SOLANA_RPC_URL=
